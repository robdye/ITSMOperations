{
  "name": "itsm-durable-functions",
  "version": "1.0.0",
  "private": true,
  "main": "dist/**/*.js",
  "description": "Azure Durable Functions for ITSM stateful workflows — replaces node-cron",
  "scripts": {
    "build": "tsc",
    "start": "func start",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/functions": "^4.16.0",
    "durable-functions": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.8",
    "@types/node": "^25.9.2"
  }
}
